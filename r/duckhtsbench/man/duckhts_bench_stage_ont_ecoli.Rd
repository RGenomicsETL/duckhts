% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ont_ecoli.R
\name{duckhts_bench_stage_ont_ecoli}
\alias{duckhts_bench_stage_ont_ecoli}
\title{Stage the Oxford Nanopore E. coli K-12 MG1655 Alignment Benchmark Input}
\usage{
duckhts_bench_stage_ont_ecoli(
  fetch = TRUE,
  threads = 8L,
  minimap2 = Sys.which("minimap2"),
  samtools = duckhts_bench_samtools()
)
}
\arguments{
\item{fetch}{Whether to download a missing or invalid source.}

\item{threads}{Aligner, sort and index threads.}

\item{minimap2}{Path to \code{minimap2}.}

\item{samtools}{Path to \code{samtools}. By default, use the executable on \code{PATH},
falling back to the optional RBCFTools package's bundled executable.}
}
\value{
Named cache paths \code{reference} (FASTA), \code{reads} (FASTQ archive) and
\code{bam} (the input).
}
\description{
Downloads ENA run \code{ERR14686255} (25,950 MinION reads of an E. coli K-12
MG1655 derivative, study PRJEB86481) and the NCBI RefSeq assembly FASTA
\code{GCF_000005845.2_ASM584v2_genomic.fna.gz}, verifies each against the
identity the registry pins (ENA's published MD5 and byte size for the reads,
NCBI's published MD5 for the archive, SHA-256 and byte size for the
uncompressed FASTA), and derives the coordinate-sorted, indexed BAM that
\code{benchmark_cigar_aligned_blocks.Rmd} reads by aligning with
\verb{minimap2 -x map-ont} and sorting with \code{samtools}. Network access occurs only
in this explicit staging step; with \code{fetch = FALSE} both sources must already
be cached, which is how the report renders. A cached BAM that passes
\verb{samtools quickcheck} is reused when its receipt's reference and read SHA-256
identities match the verified sources. Missing identities or changed inputs
require derivation; tool-version changes alone do not. \code{minimap2} is needed
only to derive the BAM.
}
